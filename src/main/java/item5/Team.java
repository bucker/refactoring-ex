package item5;

/**
 * Replace Array with Object
 * 1. Add Performance class, put `row` in it
 * 2. Find the method that use the row, replace with Performance
 * 3. Add getter/setter in Performance
 * 4. Set fields to private
 */
public class Team {

    // [0]: name
    // [1]: wins
    // [2]: loses
    public String[] row = new String[3];

}
